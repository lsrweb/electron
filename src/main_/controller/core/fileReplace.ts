import { copyFileSync } from "fs-extra";

export function fileReplace(originalFilePath: string, tempFilePath: string, replaceFilePath: string) {
  try {
    //
  } catch (error) {
    console.error(error);
  }
}
